# Structured Export — Lightroom Classic Plugin

One-invocation export from Lightroom Classic to a structured iCloud folder tree across three preset sizes (print, portfolio, web), with full IPTC metadata. See `docs/plans/structured-export-plugin.md` and `docs/lightroom-export-spec.md` for the full design. Install instructions are filled out in Step 10.
